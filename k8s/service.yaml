apiVersion: v1
kind: Service
metadata:
  name: linkpulse-api
  labels:
    app: linkpulse-api
spec:
  type: LoadBalancer
  selector:
    app: linkpulse-api
  ports:
    - name: http
      port: 80
      targetPort: 8080