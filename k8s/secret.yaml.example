apiVersion: v1
kind: Secret
metadata:
    name: linkpulse-secret
type: Opaque
data:
    # 範例檔案，實際使用時複製成 secret.yaml 並填入真實的 base64 編碼值
    # 產生方式：echo -n "你的完整連線字串" | base64
    DATABASE_URL: <base64編碼後的DATABASE_URL，範例值不代表真實密碼>
